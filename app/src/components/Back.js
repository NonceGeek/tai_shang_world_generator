export default function Back() {
  return (
    // <!-- Back to generate map inputs -->
    <div className="form-control flex hidden" id="back">
      <button
        className="btn btn-secondary btn-outline mx-5 my-5"
        id="back-button"
      >
        BACK
      </button>
    </div>
  );
}